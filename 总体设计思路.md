# 总体设计思路

> 本文档讲解项目的顶层设计——为什么这样组织代码、为什么选择这些算法、各个模块如何协作以及背后的权衡取舍。

---

## 一、架构设计：分层与解耦

### 1.1 核心问题

写 GUI 程序最常见的问题是**逻辑和界面纠缠在一起**：

```python
# ❌ 常见的"面条代码"——逻辑、界面、数据全混在一个类里
class App:
    def on_submit(self):
        answer = int(self.entry.get())           # 读界面
        if self.current_op == "+":
            correct = self.a + self.b             # 业务逻辑
        elif self.current_op == "-":
            correct = self.a - self.b
        self.label.config(text=f"得分: {score}")  # 更新界面
        self.next_question()                       # 状态 + 界面 + 逻辑
```

这种写法的问题：
- **无法独立测试**：必须启动 GUI 才能验证加法逻辑是否正确
- **修改困难**：换界面框架（如从 tkinter 换成 PyQt）需要重写全部代码
- **逻辑散落**：同样的判断可能出现在多处，改一处漏一处

### 1.2 解决方案：三层分离

本项目采用**三层架构**，严格规定依赖方向：

```
┌─────────────────────────────────┐
│           视图层 (UI)           │  ← 只管"画什么"
│  settings_frame / practice_frame │     依赖业务层
│  / results_frame / main_window  │
├─────────────────────────────────┤
│          业务逻辑层              │  ← 只管"怎么做"
│  practice_session (状态机)       │     依赖核心层
├─────────────────────────────────┤
│          核心层 (纯数据)         │  ← 只管"数据是什么"
│  question_engine / Question     │     不依赖任何上层
│  / constants / file_generator   │
└─────────────────────────────────┘
```

**最重要的规则：依赖方向是单向的、向下的。**

- `question_engine.py` 不 import 任何 `ui/*` 或 `practice_session`
- `practice_session.py` 不 import 任何 `ui/*`
- `ui/*` 可以 import 下面所有层

**验证这个设计是否干净的方法**：能否在命令行中单独测试核心层和业务层？本项目的 `test_runner.py` 证明了这一点——300+ 项测试全部在无 GUI 环境下运行。

### 1.3 模块职责划分的思考

设计时面临一个抉择：**题目生成和会话管理是分还是合？**

| 方案 | 描述 | 评价 |
|------|------|------|
| 合在一起 | 一个 `Game` 类既生成题目又管理计时计分 | 简单，但"生成题目到文件"功能会很难复用 |
| 分开（本项目） | `QuestionEngine` 只管生成，`PracticeSession` 只管流程 | 多一个文件，但每个功能都清晰独立 |

选择分开的理由：
- `FileGenerator` 只需要题目，不需要会话——它直接调用 `QuestionEngine`
- 如果要添加"考试模式"（不同规则），可以复用 `QuestionEngine` 而新建一个 `ExamSession`
- 测试时生成题目和测试会话可以独立进行

---

## 二、算法设计：从"验证"转向"构造"

### 2.1 两种算法哲学

```
哲学 A：生成 → 验证 → 拒绝重试（Generate and Test）
哲学 B：约束 → 构造 → 必然合法（Construct by Constraint）
```

本项目选择哲学 B。下面详细对比。

### 2.2 除法：为什么"生成再验证"不可靠

**方案 A（生成 + 重试）：**
```python
while True:
    a = random.randint(1, 20)
    b = random.randint(1, 20)
    if b != 0 and a % b == 0:    # 验证：无除零且整除
        break                      # 不合规则重试
```
看似可行，但存在隐藏问题：

1. **效率不可控**：`a % b == 0` 的概率约为 `1/b`。当 `b` 较大时（如 b=17），只有约 6% 的 (a, b) 组合能满足整除条件，可能需要大量重试。

2. **分布不均**：重试法会让"容易被整除的 a"出现频率远高于"难被整除的 a"。对于 `b=9`，a 可能是 9, 18, 27, 36... 但 a=20 永远不会出现。题目不再均匀随机。

3. **极端情况**：如果范围设置不合理（如 a∈[1,5], b∈[6,10]），根本不存在合法的 (a,b) 对，程序会死循环。

**方案 B（逆生成，本项目）：**
```python
b = random.randint(1, div_max)    # 选除数
c = random.randint(1, div_max)    # 选商
a = b * c                          # 构造被除数
# 此时 a ÷ b = c 天然整除，无除零
```

优势：
- **零重试**：每次一定生成合法题目
- **均匀分布**：b 和 c 都在范围内均匀随机
- **安全上限**：a 的最大值为 `div_max²`，可以设上限拒绝过大的 a

### 2.3 减法：同样的构造思路

减法不是"随机取两个数然后判断差是否 ≥ 0"，而是**先定被减数，再限制减数范围**：

```python
a = random.randint(min, max)     # 被减数任意取
b = random.randint(min, a)       # 减数 ≤ 被减数 → 差 ≥ 0
```

### 2.4 这个算法选择的启示

很多"随机生成"类问题的通用思路：

| 问题 | 传统做法 | 构造做法 |
|------|----------|----------|
| 生成数独 | 随机填数字再验证 | 先生成完整解再挖空 |
| 生成迷宫 | 随机放墙再验证连通 | 用并查集逐步打通 |
| 生成不重复随机数 | 随机取值查重 | Fisher-Yates 洗牌 |
| 生成算术题 | 随机取数验证整除 | **逆生成**（本项目） |

**核心原则：与其随机生成再验证，不如在约束空间内直接构造。**

---

## 三、数据流设计

### 3.1 数据流向图

```
用户操作                     数据                     界面反馈
────────                    ──────                    ────────

勾选运算类型 ──→  config dict ──→ QuestionEngine ──→ [无界面变化]
调整数值范围 ──→  config dict ──→ QuestionEngine ──→ [无界面变化]
点击"开始练习" ─→  config dict ──→ QuestionEngine ──→ 切换到练习标签页
                              ──→ 生成题目列表
                              ──→ PracticeSession.start()
                                     │
                                     ↓
                            显示第1题 ← current_question
                                     │
用户输入答案 ──→  submit_answer() ──→ 显示"✓正确"或"✗错误"
                                     │
                          800ms后自动 → advance()
                                     │
                            显示第2题 ← current_question
                                     │
                            ... 循环 ...
                                     │
                                     ↓  (全部答完 或 时间到)
                                     │
                            PracticeSession.is_complete = True
                                     │
                            ResultsFrame.display() ← 切换到结果标签页
                                     │
                                     ↓
                              显示得分、正确率、错题回顾
```

### 3.2 关键设计：config dict 作为"合同"

各模块之间通过一个 `config` 字典传递参数：

```python
config = {
    "operations": ["+", "-", "x", "÷"],
    "addsub_min": 1, "addsub_max": 20,
    "mul_min": 1, "mul_max": 9,
    "div_min": 1, "div_max": 9,
    "mode": "fixed",
    "time_limit": 0,
    "question_count": 20,
}
```

**为什么用 dict 而不是多个独立参数？**

| 方案 | 优点 | 缺点 |
|------|------|------|
| 多个参数 `def f(op, min_a, max_a, ...)` | 类型明确 | 参数爆炸（8+ 个），调用时容易顺序错误 |
| 独立参数对象 `class Config` | 类型安全 | 过度设计——这个项目不需要 Config 的方法 |
| **dict（本项目）** | 灵活、可序列化、各模块只取自己需要的字段 | 没有编译期检查 |

对于这个规模的项目，dict 是最合适的选择——它简单、灵活，且 `SettingsFrame.get_config()` 和 `QuestionEngine(config)` 接口对齐得很好。

### 3.3 数据不可变性

`Question` 对象（dataclass）一旦创建就不修改：

```python
@dataclass
class Question:
    operand_a: int        # 不可变
    operand_b: int        # 不可变
    operator: str         # 不可变
    correct_answer: int   # 不可变
    display_text: str     # 不可变
```

**可变的只有 `PracticeSession` 的状态**（`_current_index`、`_correct_count` 等）。将"不变的题目数据"和"变化的状态"分离，避免了"修改题目时不小心改坏了答案"之类的 bug。

---

## 四、状态管理设计

### 4.1 为什么显式管理状态

GUI 程序的时间线不是线性的：

```
启动 → [等待用户操作] → 点击按钮 → [等待输入] → 提交 → ...
```

用户可能在任意时刻做任意操作——包括"不该做的操作"：

- 反馈显示期间连按 Enter
- 练习已结束后点"提交"
- 计时已到后继续输入

如果不显式管理状态，这些操作会导致崩溃或逻辑错误。

### 4.2 双重防护

本项目在两个层面对状态进行了防护：

**业务层（`PracticeSession`）：**
```python
def submit_answer(self, answer):
    if self._finished:            # ← 第一道防线
        return False, None
    # ...
```

**界面层（`PracticeFrame`）：**
```python
def _on_submit(self):
    if self._session.is_complete: # ← 第二道防线
        return
    # ...
    self.answer_entry.configure(state="disabled")  # 禁用输入
    self.submit_btn.configure(state="disabled")    # 禁用按钮
```

两层防护的理由：即使 UI 层有 bug 漏过去了，业务层也会拒绝；即使业务层的 `_finished` 被绕过了，UI 的控件禁用也阻止了用户操作。

### 4.3 状态转换图

```
                    start()
    ┌──────────────────┐
    │    未开始         │
    └────────┬─────────┘
             │
             ↓
    ┌──────────────────┐
    │    进行中         │ ← 可答题、计时运行中
    │                  │
    │  submit_answer() │ ← 处理答案、记录对错
    │  advance()       │ ← 切换到下一题
    │  check_expiry()  │ ← 检查是否超时
    └──┬───────┬───────┘
       │       │
       │       │ mark_finished()（用户点"提前结束"）
       │       │
       ↓       ↓
    ┌──────────────────┐
    │    已结束         │ ← 拒绝一切操作
    │                  │
    │  get_wrong()     │ ← 只读操作仍可用
    │  get_results()   │
    │  score / accuracy│
    └──────────────────┘
```

一旦进入"已结束"状态，所有写操作都被拒绝，只有读操作可用——这保证了结果数据的完整性。

---

## 五、计时功能的双层设计

### 5.1 问题

计时需要同时满足两个需求：
1. **精确判断**何时超时（用于结束练习）
2. **实时显示**倒计时（用于用户感知）

### 5.2 为什么两者不能共用一个机制

`tkinter` 的 `after(1000, func)` **不精确**：
- 如果前一帧渲染耗时 50ms，`func` 实际在 1050ms 后触发
- 累积 60 次触发后，误差可达 ±3 秒
- 用触发次数 × 1000 来判断超时，3 分钟的练习实际可能跑了 3 分 5 秒或 2 分 55 秒

### 5.3 解决方案

```python
# 精确计时：用系统单调时钟
self._start_time = time.monotonic()                         # 记录起点

def elapsed_seconds(self):
    return int(time.monotonic() - self._start_time)         # 计算时间差

# 显示更新：用 after 定时器（允许误差）
def _start_timer_tick(self):
    self._update_display()                                    # 刷新显示
    self._timer_id = self.after(1000, self._start_timer_tick) # 约1秒后再刷新

# 超时判断：用精确计时
def check_timed_expiry(self):
    return self.elapsed_seconds >= self.time_limit           # monotonic 差值，精确
```

**显示可能差几百毫秒，但超时判断是精确的。**

---

## 六、文件生成的格式设计

### 6.1 TXT 格式：面向打印

**设计目标**：家长可以直接打印出来给孩子做。

```
小学生算术练习题
生成日期: 2026-06-28
========================================

加法（共 5 题）
------------------------------
    1.   12 + 7 = ______
    2.    5 + 9 = ______
    ...

减法（共 5 题）
------------------------------
    1.   18 - 6 = ______
    ...

========================================
加油！认真完成每一道题！
```

**设计考量：**
- **按运算类型分组**：方便孩子集中练习同一类题型
- **序号对齐**：`1.` 到 `99.` 保证两位数也能对齐
- **留空 `______`**：打印后手写填答案
- **首尾鼓励语**：正向激励

### 6.2 CSV 格式：面向数据处理

```csv
序号,类型,题目,正确答案
1,加法,12 + 7,19
2,加法,5 + 9,14
```

**设计考量：**
- 用 UTF-8-sig（带 BOM）编码，确保 Excel 双击打开不乱码
- 包含正确答案列，方便家长批改或导入其他系统

---

## 七、可扩展性考虑

虽然没有过度设计，但架构预留了扩展空间：

| 未来需求 | 需要改动的文件 | 改动量 |
|----------|---------------|--------|
| 添加"混合运算"题型（如 `3 + 5 × 2`） | `question_engine.py` 新增生成方法 | 一个文件 |
| 支持负数范围 | `question_engine.py` 修改 `_generate_subtraction` + `constants.py` | 两个文件 |
| 添加声音提示（答对/答错） | `practice_frame.py` 添加 `playsound` 调用 | 一个文件 |
| 换用 PyQt 界面框架 | 重写 `ui/*`，其他文件不动 | 仅 UI 层 |
| 多用户成绩记录 | 新增 `history.py`，`results_frame.py` 调用 | 新增 + 一处调用 |
| Web 版（Flask 后端） | 复用 `question_engine.py` + `practice_session.py`，重写 UI | 核心逻辑完全复用 |

这种"改一个功能只动一个文件"的特性，正是分层架构的核心价值。

---

## 八、设计取舍总结

| 决策 | 选择了 | 放弃了 | 理由 |
|------|--------|--------|------|
| 界面框架 | tkinter | PyQt / PySide / wxPython | 零依赖，适合分发给小学生家长 |
| 架构 | 三层分离 | 单文件 | 可测试、可复用、可扩展 |
| 除法算法 | 逆生成 | 随机+验证 | 零重试、均匀分布、无条件死循环 |
| 减法算法 | 逆生成 | 绝对值 | 符合"被减数 ≥ 减数"的自然理解 |
| 参数传递 | dict | 独立参数 / 参数类 | 简单灵活，规模适合 |
| 计时方案 | monotonic + after | 纯 after 计数 | 精确性与实时性兼顾 |
| 标签页管理 | 练习中锁定 | 允许自由切换 | 防止操作干扰 |
| 文件编码 | UTF-8-sig (CSV) | UTF-8 | Excel / 记事本中文兼容 |
| 状态管理 | 显式状态机 | 隐式布尔变量 | 可预测、安全、易测试 |
