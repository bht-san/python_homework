# 程序实现讲解 —— 结合功能点逐层剖析

> 本文档按照用户可见的功能点，逐个讲解背后的代码实现。每个功能点包含"用户看到什么"→"代码做了什么"→"关键实现细节"三层分析。

---

## 功能一：启动程序，看到主窗口

### 用户视角

双击运行后，出现一个 800×600 的窗口，标题是"小学生加减乘除运算练习器"，窗口居中显示，底部有状态栏，三个标签页可切换。

### 代码路径

```
main.py → MainWindow.__init__()
```

### 实现细节

**入口文件 [main.py](main.py:8-11)**：

```python
def main():
    root = tk.Tk()              # 创建 Tk 根窗口
    app = MainWindow(root)      # 将根窗口交给 MainWindow 管理
    root.mainloop()             # 启动事件循环
```

`tk.Tk()` 创建一个操作系统级别的窗口。`mainloop()` 是关键——它让程序进入**事件循环**，此后程序不再按代码顺序线性执行，而是等待用户操作（点击、按键）并触发对应的回调函数。

**窗口初始化 [ui/main_window.py:21-48]**：

```python
self.root.title("小学生加减乘除运算练习器")
self.root.geometry("800x600")
self.root.resizable(False, False)   # 禁止拖拽缩放（保持布局稳定）
self._center_window()               # 计算屏幕中心坐标并定位
```

`_center_window()` 的计算逻辑：
```python
x = (屏幕宽度 - 800) // 2
y = (屏幕高度 - 600) // 2
self.root.geometry(f"800x600+{x}+{y}")   # "宽x高+左+上"
```

**标签页创建**：
```python
self.notebook = ttk.Notebook(root)           # 标签页容器
self.notebook.add(settings_frame, text="练习设置")  # 标签页0
self.notebook.add(practice_frame, text="开始练习")  # 标签页1
self.notebook.add(results_frame,  text="练习记录")  # 标签页2
```

三个标签页对应的 Frame 在 `__init__` 中一次性全部创建，切换标签页时只是显示/隐藏，不会重新创建——这是 GUI 程序的常见做法，避免重复初始化开销。

---

## 功能二：练习设置 —— 选择运算类型和数值范围

### 用户视角

在"练习设置"标签页，用户可以：
- 勾选四种运算（加减乘除），默认全选
- 设置加法/减法范围（默认 1-20）
- 设置乘法范围（默认 1-9，即九九乘法表）
- 设置除法范围（默认 1-9）
- 选择练习模式：定量（默认 20 题）或计时（默认 3 分钟）

### 代码路径

```
ui/settings_frame.py → SettingsFrame._build_ui()
                    → SettingsFrame.get_config()
                    → SettingsFrame.validate()
```

### 实现细节

**运算类型复选框 [ui/settings_frame.py:37-47]**：

```python
self.op_vars = {}                       # 存储 BooleanVar 的字典
for key, label in OPERATIONS.items():   # OPERATIONS = {"+":"加法", "-":"减法", ...}
    var = tk.BooleanVar(value=True)     # 默认全部勾选
    self.op_vars[key] = var
    cb = ttk.Checkbutton(ops_inner, text=f"{label} ({key})", variable=var)
```

每个复选框绑定一个 `BooleanVar`。读取时遍历字典即可获取用户选择：
```python
operations = [k for k, v in self.op_vars.items() if v.get()]
# 结果示例：["+", "-", "x"]（用户取消了"÷"）
```

**范围设置 [ui/settings_frame.py:57-70]**：

三组范围使用同一个 `_make_range_row` 方法创建——这是 DRY（Don't Repeat Yourself）原则：
```python
@staticmethod
def _make_range_row(parent, label_text, default_min, default_max, row):
    """创建 '标签: 最小值 [spin] 最大值 [spin]' 的一行"""
    min_var = tk.IntVar(value=default_min)     # 绑定整数变量
    max_var = tk.IntVar(value=default_max)
    Spinbox(parent, from_=0, to=999, textvariable=min_var, width=6)
    Spinbox(parent, from_=1, to=999, textvariable=max_var, width=6)
    return min_var, max_var
```

`Spinbox` 自带上下箭头，限制输入范围 `from_=0, to=999`，用户无法输入非法值。

**模式切换联动 [ui/settings_frame.py:104-109]**：

```python
def _on_mode_change(self):
    if self.mode_var.get() == MODE_FIXED:
        self.count_spin.configure(state="normal")    # 定量模式：数量可调
        self.time_spin.configure(state="disabled")   #           ：时长禁用
    else:
        self.count_spin.configure(state="disabled")  # 计时模式：数量禁用
        self.time_spin.configure(state="normal")     #           ：时长可调
```

这是一个细节交互设计：两个 Spinbox 同时只有一个可修改，避免用户困惑（"我到底是在定题目数量还是定时长？"）。Radiobutton 的 `command=self._on_mode_change` 在每次切换时触发。

**配置读取与校验 [ui/settings_frame.py:114-145]**：

```python
def get_config(self) -> dict:
    return {
        "operations": [k for k, v in self.op_vars.items() if v.get()],
        "addsub_min": self.addsub_min_var.get(),   # 从 IntVar 读取当前值
        "addsub_max": self.addsub_max_var.get(),
        # ... 其他字段
        "mode": self.mode_var.get(),
        "time_limit": self.time_var.get() * 60,    # 分钟 → 秒
        "question_count": self.count_var.get(),
    }

def validate(self) -> list[str]:
    errors = []
    if not config["operations"]:              # 没选任何运算
        errors.append("请至少选择一种运算类型。")
    if config["addsub_min"] > config["addsub_max"]:  # 最大值小于最小值
        errors.append("加法/减法：最小值不能大于最大值。")
    # ... 更多校验
    return errors
```

`get_config()` 和 `validate()` 分离的原因是：文件生成和开始练习共用同一套校验逻辑。

---

## 功能三：开始练习 —— 题目生成

### 用户视角

点击"开始练习"后，窗口自动切换到"开始练习"标签页，显示第一道题，计时开始。

### 代码路径

```
MainWindow._on_start_practice()
  → QuestionEngine(config)
  → QuestionEngine.generate_batch(count)
  → PracticeSession(questions, mode, time_limit)
  → PracticeFrame.start_session(session)
```

### 实现细节

**第一步：根据配置创建引擎并生成题目 [ui/main_window.py:98-118]**：

```python
def _on_start_practice(self, config):
    engine = QuestionEngine(config)           # 创建引擎

    if config["mode"] == MODE_TIMED:
        batch_count = 500                     # 计时模式：生成大量题目
    else:
        batch_count = config["question_count"] # 定量模式：精确生成所需数量

    session = PracticeSession(
        engine.generate_batch(batch_count),   # 调用引擎批量生成
        mode=config["mode"],
        time_limit=config["time_limit"],
        question_count=batch_count,
    )
    self.practice_frame.start_session(session)  # 交给界面
```

计时模式生成 500 题的原因：用户可能在 3 分钟内做 30-50 题，500 题足够任何情况，避免"题库用完"。

**第二步：单题生成策略 [question_engine.py:49-56]**：

```python
def generate_one(self):
    op = random.choice(self.operations)    # 随机选一种运算
    if op == "+":   return self._generate_addition()
    if op == "-":   return self._generate_subtraction()
    if op == "x":   return self._generate_multiplication()
    if op == "÷":   return self._generate_division()
```

每种运算的生成方法独立，便于单独理解和修改。

**第三步：批量生成与去重 [question_engine.py:58-72]**：

```python
def generate_batch(self, count, allow_duplicates=False):
    questions = []
    seen = set()                              # 用于去重的哈希集合

    for _ in range(count):
        for _retry in range(100):             # 最多重试 100 次
            q = self.generate_one()
            key = (q.operand_a, q.operand_b, q.operator)
            if allow_duplicates or key not in seen:
                break
        seen.add(key)
        questions.append(q)
    return questions
```

`set` 的查找是 O(1)（哈希表），100 次重试上限防止了极端情况下的无限循环（范围太小导致所有合法组合都已用完时）。

**第四步：具体运算的生成逻辑**：

加法和乘法直接随机取操作数即可，不需要特殊处理。

减法和除法使用了**逆生成法**——这是本项目最核心的算法设计（详见 [总体设计思路.md](总体设计思路.md) 第二章）：

```python
# 除法：先生成除数和商，反推被除数
def _generate_division(self):
    b = random.randint(1, div_max)        # 除数 ≥ 1（杜绝除零）
    c = random.randint(1, div_max)        # 商
    a = b * c                              # 被除数 = 除数 × 商
    if a <= 200:                           # 上限保护
        return Question(a, b, "÷", c, f"{a} ÷ {b} = ?")

# 减法：先生成被减数，限制减数范围
def _generate_subtraction(self):
    a = random.randint(min_val, max_val)
    b = random.randint(min_val, a)         # 减数 ≤ 被减数 → 差 ≥ 0
    return Question(a, b, "-", a - b, f"{a} - {b} = ?")
```

---

## 功能四：答题与即时反馈

### 用户视角

题目以大字体显示在中央，用户在输入框输入答案，按回车或点"提交"。0.8 秒后，绿色显示"✓ 正确！太棒了！"或红色显示"✗ 很遗憾，正确答案是 XX"，然后自动跳到下一题。

### 代码路径

```
PracticeFrame._on_submit()
  → PracticeSession.submit_answer()
  → PracticeFrame._advance_or_finish()
```

### 实现细节

**提交按钮的智能启用 [ui/practice_frame.py:144-154]**：

```python
def _on_input_change(self, event):
    val = self.answer_var.get().strip()
    if val == "" or val == "-":
        self.submit_btn.configure(state="disabled")  # 空输入或仅负号 → 禁用
        return
    try:
        int(val)                                      # 尝试转整数
        self.submit_btn.configure(state="normal")     # 合法 → 启用
    except ValueError:
        self.submit_btn.configure(state="disabled")   # 非数字 → 禁用
```

绑定到 `<KeyRelease>`（每次松开按键时触发），实现**实时输入校验**——用户在输入框里打字时，提交按钮动态启用/禁用。这比点击提交后再弹错误提示体验更好。

**答案判断 [practice_session.py:74-95]**：

```python
def submit_answer(self, answer):
    q = self.active_questions[self._current_index]  # 当前题目
    self._answers[self._current_index] = answer     # 记录用户答案

    if answer == q.correct_answer:
        self._correct_count += 1
        return True, None                            # (正确, 无需正确答案)
    else:
        self._wrong_records.append({                 # 记录错题
            "index": self._current_index + 1,
            "question": q.display_text,
            "user_answer": answer,
            "correct_answer": q.correct_answer,
        })
        return False, q.correct_answer               # (错误, 正确答案)
```

返回 `(是否答对, 正确答案或None)` 的元组。答对时正确答案为 `None`，答错时返回正确答案——这样界面层不需要再去题目对象里找答案。

**反馈延时与自动推进 [ui/practice_frame.py:156-187]**：

```python
def _on_submit(self):
    # ... 判断对错，显示反馈 ...

    self.answer_entry.configure(state="disabled")   # 禁用输入
    self.submit_btn.configure(state="disabled")     # 禁用按钮
    self.after(800, self._advance_or_finish)         # 800ms 后自动推进

def _advance_or_finish(self):
    if self._session.check_timed_expiry():           # 先检查是否超时
        self._finish_session()
        return

    next_q = self._session.advance()                 # 移动到下一题
    if next_q is None:                               # 没有下一题了
        self._finish_session()
        return

    # 恢复界面，显示新题目
    self.feedback_label.configure(text="")
    self.answer_var.set("")
    self.answer_entry.configure(state="normal")
    self.answer_entry.focus_set()                    # 自动聚焦输入框
    self._show_question(next_q)
```

800ms 的延时是一个**用户体验设计**：足够用户看清反馈，又不会让人觉得等待太久。在此期间输入框和按钮被禁用，防止连击。

---

## 功能五：计时模式 —— 倒计时与超时

### 用户视角

练习页右上角显示"⏱ 剩余 2:35"，时间不足 30 秒时出现红色警告"⚠ 剩余时间不足 30 秒！"。时间归零后自动结束练习，跳转到结果页。

### 代码路径

```
PracticeFrame._start_timer_tick()
  → PracticeFrame._update_timer_display()
  → PracticeSession.check_timed_expiry()
```

### 实现细节

**双层计时方案 [ui/practice_frame.py:214-242]**：

```python
def _start_timer_tick(self):
    """显示刷新 —— 每约1秒触发一次"""
    if self._session.is_complete:
        return
    self._update_timer_display()
    self._timer_id = self.after(1000, self._start_timer_tick)  # 递归预约
```

`after(1000, ...)` 不会精确地在 1000ms 后触发（可能有几十毫秒误差），所以它只用来**刷新显示**。

**精确超时判断在 `_update_timer_display` 中**：

```python
def _update_timer_display(self):
    remaining = self._session.remaining_seconds       # 用 monotonic 差值计算
    mins, secs = divmod(remaining, 60)                # 秒 → 分:秒
    self.timer_label.configure(text=f"⏱ 剩余 {mins}:{secs:02d}")

    if remaining <= 30:
        self.countdown_label.configure(               # 30秒警告
            text="⚠ 剩余时间不足 30 秒！", fg="red")

    if self._session.check_timed_expiry():            # 精确判断超时
        self.after(100, self._finish_session)         # 给界面 100ms 缓冲后结束
```

`elapsed_seconds` 使用 `time.monotonic()` 差值，不受系统时间调整影响，不累积 `after` 误差。

```python
# practice_session.py
@property
def elapsed_seconds(self):
    return max(0, int(time.monotonic() - self._start_time))  # 精确！

def check_timed_expiry(self):
    if self.mode != MODE_TIMED:
        return False
    if self.elapsed_seconds >= self.time_limit:
        self._finished = True                     # 标记结束
        return True
    return False
```

---

## 功能六：定量模式 —— 进度追踪

### 用户视角

练习页左上角显示"第 3/20 题"，右上角显示"⏱ 用时 0:42"。做完所有题目后自动结束。

### 实现细节

定量模式没有倒计时，但**仍然计时**——显示的是"已用时间"而非"剩余时间"：

```python
# practice_frame.py
if session.mode == MODE_TIMED:
    self.timer_label.configure(text=f"⏱ 剩余 {mins}:{secs:02d}")
else:
    self.timer_label.configure(text=f"⏱ 用时 {mins}:{secs:02d}")  # 正面计时
```

进度推进在 `advance()` 中管理 [practice_session.py:97-105]：

```python
def advance(self):
    self._current_index += 1
    if self.mode == MODE_FIXED:
        if self._current_index >= len(self.active_questions):
            self._finished = True
            return None          # 返回 None 表示"没有下一题了"
    return self.current_question
```

---

## 功能七：练习结果展示

### 用户视角

做完练习后自动切换到"练习记录"标签页，显示：
- 正确数/总答题数/正确率
- 星级评价（⭐⭐⭐⭐⭐ 到 ⭐）
- 用时
- 错题回顾列表

### 实现细节

**成绩计算 [practice_session.py:139-157]**：

```python
@property
def score(self) -> tuple[int, int, int]:
    answered = self._current_index
    wrong = answered - self._correct_count
    total = len(self.active_questions)
    unanswered = total - answered
    return self._correct_count, wrong, max(0, unanswered)

@property
def accuracy(self) -> float:
    if self._current_index == 0:
        return 0.0
    return self._correct_count / self._current_index  # 正确数 / 已答题数
```

注意准确率的分母是**已答题数**而非总题数——这是合理的设计，因为计时模式下的总题数（500）远超实际能答的题数。

**星级评价 [ui/results_frame.py:119-128]**：

```python
@staticmethod
def _get_stars(accuracy):
    if accuracy >= 95:   return "⭐⭐⭐⭐⭐ 完美！"
    elif accuracy >= 85:  return "⭐⭐⭐⭐ 优秀！"
    elif accuracy >= 70:  return "⭐⭐⭐ 良好"
    elif accuracy >= 60:  return "⭐⭐ 继续加油"
    else:                 return "⭐ 多加练习"
```

**错题滚动列表 [ui/results_frame.py:94-117]**：

错题列表使用 Canvas + Scrollbar 实现滚动——因为错题数量不确定，可能超过窗口高度：

```python
self.review_canvas = tk.Canvas(container, height=200)
scrollbar = ttk.Scrollbar(container, command=self.review_canvas.yview)
self.review_inner = ttk.Frame(self.review_canvas)       # 内容放在这个 Frame 里
self.review_canvas.create_window((0,0), window=self.review_inner, anchor="nw")
self.review_canvas.configure(yscrollcommand=scrollbar.set)
```

工作原理：Canvas 是一个可滚动的画布，内部放置一个 Frame。Frame 内容超出 Canvas 可见区域时，Scrollbar 控制 Canvas 的 `yview` 偏移。

**每道错题的渲染**：
```python
for record in wrong_qs:
    text = (f"第 {record['index']} 题  "
            f"{record['question']}  "
            f"你的答案：{record['user_answer']}  "
            f"正确答案：{record['correct_answer']}")
    ttk.Label(row, text=text, foreground="red").pack()
```

---

## 功能八：生成题目到文件

### 用户视角

在设置页点击"📄 生成题目到文件"，弹出保存对话框，选择路径和格式（TXT/CSV），生成后可打印。

### 代码路径

```
MainWindow._on_generate_file()
  → FileGenerator.generate_txt() 或 generate_csv()
```

### 实现细节

**文件对话框 [ui/main_window.py:200-210]**：

```python
path = filedialog.asksaveasfilename(
    title="保存题目文件",
    defaultextension=".txt",
    filetypes=[("文本文件", "*.txt"), ("CSV 文件 (含答案)", "*.csv")],
    initialfile="算术练习题.txt",
)
```

`filedialog.asksaveasfilename` 打开系统原生保存对话框，返回用户选择的路径。用户取消时返回空字符串。

**TXT 输出 [file_generator.py:18-46]**：

按运算类型分组输出：
```python
# 先分组
groups = {"+": [], "-": [], "x": [], "÷": []}
for q in questions:
    groups[q.operator].append(q)

# 再按组输出
for op, label in [("+","加法"), ("-","减法"), ("x","乘法"), ("÷","除法")]:
    qs = groups[op]
    if not qs:
        continue
    f.write(f"\n{label}（共 {len(qs)} 题）\n")
    for i, q in enumerate(qs, 1):
        f.write(f"  {i:>3}.   {q.display_text.replace('?', '______')}\n")
```

`{i:>3}` 是 Python 的格式化语法，表示"右对齐占 3 格"——保证个位数和十位数的序号对齐。

**CSV 输出 [file_generator.py:49-72]**：

```python
with open(output_path, "w", encoding="utf-8-sig", newline="") as f:
    writer = csv.writer(f)
    writer.writerow(["序号", "类型", "题目", "正确答案"])
    for i, q in enumerate(questions, 1):
        writer.writerow([i, OPERATIONS[q.operator],
                         f"{q.operand_a} {q.operator} {q.operand_b}",
                         q.correct_answer])
```

`utf-8-sig` 在文件开头添加 BOM 标记，确保 Windows Excel 能正确识别中文编码。

---

## 功能九：防护机制

### 9.1 练习中锁定标签页 [ui/main_window.py:55-56]

```python
self.notebook.bind("<<NotebookTabChanged>>", self._on_tab_changed)

def _on_tab_changed(self, event):
    if self._practice_active:
        if self.notebook.index("current") != 1:
            self.notebook.select(1)    # 强制切回练习页
```

`<<NotebookTabChanged>>` 是 ttk.Notebook 的内置虚拟事件，在标签页切换后触发。配合 `_practice_active` 布尔标记，练习期间用户点击其他标签页会被立即弹回。

### 9.2 反馈期间防连击 [ui/practice_frame.py:181-184]

```python
# 提交后立即禁用输入和按钮
self.answer_entry.configure(state="disabled")
self.submit_btn.configure(state="disabled")
# 800ms 后才恢复
self.after(800, self._advance_or_finish)
```

同时 `_on_submit` 开头有第二道防线：
```python
if self._session.is_complete:    # 已结束则拒绝
    return
```

### 9.3 会话级防重复结束 [ui/practice_frame.py:251-262]

```python
def _finish_session(self):
    if self._session is None or self._session.is_complete:
        return  # 已经结束过了，不重复处理

    self._session.mark_finished()
    # ... 清理界面 ...
    self.after(500, lambda: self.on_session_end(self._session))
```

因为 `_finish_session` 可能被两个路径触发（用户点"提前结束"或定时器检测到超时），这个检查防止结果页被重复调用。

### 9.4 非法配置拦截 [ui/settings_frame.py:132-145]

```python
errors = self.validate()
if errors:
    messagebox.showwarning("设置错误", "\n".join(errors))
    return   # 阻止开始，不切换到练习页
```

在"开始练习"和"生成文件"两个按钮的回调中，**都先校验后执行**。

---

## 完整功能流程示例

以"定量模式，做 5 道加法题"为例，跟踪一次完整的代码执行路径：

```
1. 用户在设置页勾选"加法"，范围设为 1-10，选择"定量 5 题"
   → 每个控件的 IntVar/BooleanVar 随用户操作自动更新

2. 用户点击"▶ 开始练习"
   → SettingsFrame._on_start_click()
     → validate() 返回 []（无错误）
     → get_config() 返回 {"operations":["+"], "addsub_min":1, "addsub_max":10, ..., "question_count":5}
     → on_start(config) → MainWindow._on_start_practice(config)
       → QuestionEngine(config)
       → engine.generate_batch(5)
         → 循环 5 次 generate_one() → _generate_addition()
           → 每次 random.randint(1,10) 两次，求和
           → 生成 5 个 Question 对象
       → PracticeSession(questions, mode="fixed", question_count=5)
         → active_questions = questions[:5]
       → practice_frame.start_session(session)
         → session.start()（记录 _start_time）
         → _show_question(q1)
         → _start_timer_tick()（启动显示更新）
       → _switch_to_tab(1)（切换到练习标签页）
       → 显示 "1 + 7 = ?"

3. 用户输入 "8"，按 Enter
   → answer_entry 触发 <Return> → _on_submit()
     → int("8") = 8
     → session.submit_answer(8)
       → q.correct_answer = 8
       → 8 == 8 → _correct_count += 1 → return (True, None)
     → 显示 "✓ 正确！太棒了！"（绿色）
     → 禁用输入框和按钮
     → after(800, _advance_or_finish)

4. 800ms 后
   → _advance_or_finish()
     → check_timed_expiry() → False
     → advance() → _current_index = 1
     → 恢复输入框，清空反馈
     → _show_question(q2)  → 显示 "3 + 5 = ?"

5. 用户答完 5 题
   → 最后一次 _advance_or_finish()
     → advance() → _current_index = 5, 发现 >= len(active_questions)
     → _finished = True, return None
     → _finish_session()
       → after_cancel(timer_id)（停止计时）
       → mark_finished()
       → after(500, lambda: on_session_end(session))

6. 500ms 后
   → MainWindow._on_session_end(session)
     → _practice_active = False（解锁标签页）
     → results_frame.display(session)
       → 显示 "本次成绩：5/5 正确"
       → 显示 "正确率：100%"
       → 显示 "⭐⭐⭐⭐⭐ 完美！"
       → 显示 "🎉 太棒了！全部正确，没有错题！"
     → _switch_to_tab(2)（切换到结果标签页）
```

这就是整个程序从设置到完成的一条完整执行路径。
